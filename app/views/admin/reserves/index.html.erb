<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-10'>
      <h2 class="mt-4">
        <span>予約一覧</span>
      </h2>
      
      <table class="table">
        <thead>
          <tr class="bg-light">
            <th>日付</th>
            <th>時間</th>
            <th>会員ID</th>
            <th>氏名</th>
            <th>フリガナ</th>
          </tr>
        </thead>
        <tbody>
          <% @customers.each do |customers| %>
            <tr>
              <td><%= customers.id %></td>
              <td><u>
                <%= link_to admin_customer_path(customers), :style=>"color:black;" do %>
                  <%= customers.last_name %>
                  <%= customers.first_name %>
                <% end %>
              </td></u>
              <td>
                <%= customers.last_kana %>
                <%= customers.first_kana %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @customer %>
    </div>
  </div>
</div>